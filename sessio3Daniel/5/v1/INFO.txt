Desde el momento en que clicas:

entras en mousePressEvent

XPos = lastXPos = e->x(); YPos = lastYPos = e->y();

setMouseTracking(true);

  // If mouse tracking is disabled (default), the widget only receives mouse move 
  // events when at least one mouse button is pressed while the mouse is being moved.

  // If mouse tracking is enabled, the widget receives mouse move events even if no 
  // buttons are pressed.

Luego en mouseMoveEvent obtienes las X Y actuales, calculas la diferencia y segun para
donde hayamos ido, haremos una cosa u otra (en este caso, mover el triangulo)
